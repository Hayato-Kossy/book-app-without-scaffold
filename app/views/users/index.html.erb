<p style="color: green"><%= notice %></p>

<h1>Users</h1>
<a href='/'>Back</a>
<ul>
  <% @users.each do |user| %>
    <li><a href="/users/<%= user.id %>">
    <%= user.name %>
    </a>
    <%= user.age %>歳
    </li>
  <% end %>
</ul>
<%= link_to "新規登録", new_user_path %>